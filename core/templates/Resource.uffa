<?php

namespace custom\Resources;

use core\Bases\Resource;
use core\Traits\Resource\Get;
use core\Web\Api;
use core\Web\Route;

#[Route(base: "/{$machine}")]
class {$name} extends Resource
{
    use Get;

    #[Api(type: "id", method: ["get"])]
    public $id;
}
